# Build output
*.exe
*.exe~
*.dll
*.so
*.dylib

# Built binaries (this project's name, and release output)
/cli-agent-mcp
/dist/

# Windows resource objects, generated from winres/winres.json at build time.
# Not committed: they carry the release version, so a checked-in copy would go
# stale the moment the next tag is cut.
*.syso

# Test binaries / coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Transcripts from scripts/e2e.ps1 (real-agent runs; may contain prompts and output)
.e2e/
cli-agent-mcp.exe
